import { routeNames } from './route-names';

export const routes = {
    actionForm: '/' + routeNames.formOne + '/' + routeNames.actionForm,
    formOne: '/' + routeNames.formOne,
    modules: '/' + routeNames.modules,
    signIn: '/' + routeNames.signIn
};