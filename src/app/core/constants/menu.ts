import { routes } from './routes';
import { MenuItem } from '../interfaces/menu-item';

export const menu: Array<MenuItem> = [
    {
        icon: 'apps',
        title: 'Modules',
        url: routes.modules
    }
];